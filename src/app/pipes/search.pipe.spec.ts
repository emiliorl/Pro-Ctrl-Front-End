import { SearchTopicPipe } from './searchTopic.pipe';

describe('SearchTopicPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTopicPipe();
    expect(pipe).toBeTruthy();
  });
});
